# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: 🚀 Build & Publish Website

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  restart-app-pool:
   runs-on: ubuntu-latest
   env:
      server: 'iis-server.domain.com'
      pool-name: 'website-pool'

   steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: IIS stop
      # You may also reference the major or major.minor version
      uses: im-open/app-pool-action@v2.0.3
      with:
        action: 'stop'
        server: 103.125.254.20                    #${{ env.server }}
        app-pool-name: 'ci-cd.redwan.pro'           #${{ env.pool-name }}
        service-account-id: 'redwan'                #${{ secrets.iis_admin_user }}
        service-account-password: 's8dagn#$4REj'    #${{ secrets.iis_admin_password }}

